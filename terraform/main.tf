# FinOps AI Terraform Root Module
# This file will be automatically generated during deployment
# For now, we maintain the existing state for destroy operations

locals {
  project_name = var.project_name
  environment  = var.environment
  aws_region   = var.aws_region
}

# Placeholder - actual module configurations managed during deployment
