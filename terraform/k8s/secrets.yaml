apiVersion: v1
kind: Secret
metadata:
  name: finops-secrets
  namespace: finops
  labels:
    app.kubernetes.io/part-of: finops-ai
type: Opaque
stringData:
  # ── Replace these at deploy time with actual values from Terraform outputs ──
  DATABASE_URL: "postgresql://finops:CHANGE_ME@rds-endpoint:5432/finops_db"
  NEO4J_PASSWORD: "finops_neo4j"
  AWS_ACCESS_KEY_ID: ""
  AWS_SECRET_ACCESS_KEY: ""
  AWS_DEFAULT_REGION: "us-east-1"
