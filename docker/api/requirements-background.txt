# New Backend Requirements
# Add these to existing requirements.txt or requirements-dev.txt

# Redis & Caching
redis>=4.5.0
hiredis>=2.2.0  # Performance optimization

# Celery & Task Queue
celery>=5.3.0
celery-beat>=2.5.0
celery-redbeat>=2.0.0  # Redis-backed Beat scheduler

# Optional: Monitoring
flower>=2.0.0  # Celery task monitoring web UI
prometheus-client>=0.17.0  # Metrics

# Already in existing requirements but ensure versions
sqlalchemy>=2.0.0
fastapi>=0.104.0
pydantic>=2.0.0
